# Crypto Trading Research — Proposal 1
# IIT Bombay Research Internship 2026
# Author: Ria Chawak

# ── Data ──────────────────────────────────────────────────────────────
ccxt>=4.3.0                  # Binance API wrapper for OHLCV + funding rate
pandas>=2.0.0
numpy>=1.26.0
pyarrow>=14.0.0              # Parquet file format

# ── Features & Statistics ──────────────────────────────────────────────
scipy>=1.12.0
statsmodels>=0.14.0          # ADF test, Engle-Granger cointegration (Proposals 2&3)
ta>=0.11.0                   # Technical analysis library (backup for indicator checks)

# ── ML ─────────────────────────────────────────────────────────────────
lightgbm>=4.3.0
scikit-learn>=1.4.0

# ── Deep Learning (for Proposals 2&3) ──────────────────────────────────
# torch>=2.2.0               # PyTorch for LSTM (Proposal 2) — install separately with CUDA
# tensorflow>=2.16.0         # Alternative DL backend

# ── Plotting ────────────────────────────────────────────────────────────
matplotlib>=3.8.0
seaborn>=0.13.0

# ── Utilities ───────────────────────────────────────────────────────────
tqdm>=4.66.0                 # Progress bars
python-dateutil>=2.9.0

# ── Install command ─────────────────────────────────────────────────────
# pip install -r requirements.txt
# For PyTorch (with CUDA): pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121
